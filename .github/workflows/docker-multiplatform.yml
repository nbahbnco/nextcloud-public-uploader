name: Docker Build (Multi-Platform) - Release

# This workflow runs only on version tags for production releases
on:
  push:
    tags:
      - 'v*.*.*'

# A workflow run is made up of one or more jobs that can run sequentially or in parallel.
jobs:
  build-and-push-multiplatform:
    # The type of runner that the job will run on.
    runs-on: ubuntu-latest

    # Grant GITHUB_TOKEN permissions to write to GitHub Packages (GHCR).
    permissions:
      contents: read
      packages: write

    steps:
      # 1. Checkout the repository code.
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2. Set up QEMU for multi-platform builds.
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      # 3. Set up Docker Buildx, the advanced builder.
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # 4. Extract metadata (tags, labels) for Docker.
      # This action automatically creates useful tags for your image based on the Git context.
      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository }}
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=raw,value=latest,enable={{is_default_branch}}
          labels: |
            org.opencontainers.image.title=nextcloud-public-uploader
            org.opencontainers.image.description=Nextcloud Public Uploader
            org.opencontainers.image.vendor=${{ github.repository_owner }}

      # 5. Log in to the container registry.
      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # 6. Build and push the Docker image (Multi-platform for production).
      - name: Build and push Docker image (Multi-platform)
        id: build-and-push
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/arm64, linux/riscv64, linux/ppc64le, linux/s390x
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          # Pass version information as build arguments
          build-args: |
            VERSION=${{ github.ref_name }}
            COMMIT=${{ github.sha }}
            DATE=${{ github.event.head_commit.timestamp }}
          # Enable build caching to speed up future builds
          cache-from: type=gha
          cache-to: type=gha,mode=max
